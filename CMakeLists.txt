# Data-only module — no C++ sources. AzerothCore discovers this directory via
# modules/CMakeLists.txt; world SQL under data/sql/db-world/ is applied by the
# DB updater on worldserver start (see README).
